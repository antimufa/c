#include <stdio.h>
#include <stdlib.h>
 int main()
{
    /*PROGRAMA DE PRUEBA 1
    char *pc;
    char c;
    pc = &c;
    c='a';
    printf("%c ////// %d",c,pc);
    return 0;
    */

    /* PROGRAMA DE PRUEBA 2
    int y, *ip;
    y = 12;
    printf ("\nValor de y %d, de ip %d", y, ip); //sin asignar ip
    ip = &y; // LE ASIGNO LO QUE LA DIRECCION AL PUNTERO
    *ip+= 10; // LE SUMO 10 A LO QUE ESTE EN ESE PUNTERO (EN ESTE CASO, 12)
    printf ("\nValor de y %d, de *ip %d y de ip %d", y, *ip, ip);
    y = *ip + 10; // LO MISMO HACER Y+=10 YA QUE *IP APUNTA AL CONTENIDO DE Y
    printf ("\nValor de y %d, de *ip %d", y, *ip);
    *ip += 1;
    printf ("\nValor de y %d, de *ip %d", y, *ip);
    */
    return 0;
}



